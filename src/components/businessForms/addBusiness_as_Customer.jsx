import React, { Component } from 'react';
import { reduxForm, Field } from 'redux-form';

class AddBusinessAsCustomer extends Component {
    render(){
        return(
            <div></div>
        );
    }
}

export default AddBusinessAsCustomer;